# ===================
# GCP Managed Services
# ===================

# Cloud SQL PostgreSQL (Private IP)
DATABASE_HOST=10.101.0.3
DATABASE_PORT=5432
DATABASE_NAME=exit8_app
DATABASE_USER=exit8_app_user
# DATABASE_PASSWORD is fetched from GCP Secret Manager: exit8-db-password

# Memorystore Redis (Private IP)
REDIS_HOST=10.101.1.3
REDIS_PORT=6379

# ===================
# Cache Settings (2-Tier: Caffeine L1 + Redis L2)
# ===================
CAFFEINE_MAX_SIZE=1000
CAFFEINE_EXPIRE_AFTER_WRITE=300
REDIS_CACHE_TTL=600

# ===================
# Application Settings
# ===================
# APP_ENV=development
# APP_SECRET_KEY=your-secret-key

# ===================
# Grafana Settings
# ===================
GRAFANA_USER=admin
# GRAFANA_PASSWORD is fetched from GCP Secret Manager: exit8-grafana-admin

# ===================
# Wazuh Settings
# ===================
# WAZUH_INDEXER_PASSWORD is fetched from GCP Secret Manager: exit8-wazuh-credentials
# WAZUH_API_PASSWORD is fetched from GCP Secret Manager: exit8-wazuh-credentials
# WAZUH_DASHBOARD_PASSWORD is fetched from GCP Secret Manager: exit8-wazuh-credentials

# ===================
# Local Development Only (docker-compose.local.yml)
# ===================
# POSTGRES_USER=admin
# POSTGRES_PASSWORD=changeme
# POSTGRES_DB=appdb
# REDIS_PASSWORD=changeme
